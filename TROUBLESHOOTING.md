# استكشاف أخطاء النشر - دليل سريع

## ✅ قائمة التحقق السريعة

إذا فشل النشر، تحقق من هذه النقاط بالترتيب:

### 1. متغيرات البيئة
- [ ] `DATABASE_URL` محدد وينتهي بـ `?sslmode=require`
- [ ] `SESSION_SECRET` محدد (أو يتم توليده تلقائياً)
- [ ] `NODE_ENV=production` محدد
- [ ] جميع المتغيرات بدون مسافات إضافية أو أحرف خاصة

### 2. قاعدة البيانات
- [ ] قاعدة بيانات Neon تم إنشاؤها ونشطة
- [ ] رابط الاتصال صحيح ومنسوخ بالكامل
- [ ] تم تشغيل `npm run db:push` بعد النشر

### 3. البناء (Build)
- [ ] أمر البناء: `npm install && npm run build`
- [ ] أمر التشغيل: `npm start`
- [ ] لا توجد أخطاء في سجلات البناء (Build Logs)

### 4. المنفذ (Port)
- [ ] PORT محدد على 3000 أو يتم ضبطه تلقائياً
- [ ] Render يضبط PORT تلقائياً - لا تقلق إذا لم تحدده

---

## 🔴 الأخطاء الشائعة والحلول

### خطأ: "DATABASE_URL must be set"

**ما يعنيه:** التطبيق لا يجد رابط قاعدة البيانات

**الحل:**
1. تأكد من إضافة `DATABASE_URL` في Environment Variables في Render
2. تحقق من أن القيمة صحيحة ومنسوخة بالكامل من Neon
3. يجب أن يكون الرابط بهذا الشكل:
   ```
   postgresql://username:password@ep-xxx.neon.tech/dbname?sslmode=require
   ```
4. تأكد من أنه ينتهي بـ `?sslmode=require`

**كيفية الإصلاح:**
1. في Render Dashboard، افتح Web Service الخاص بك
2. اذهب إلى **Environment**
3. أضف أو عدّل `DATABASE_URL`
4. احفظ واضغط **Manual Deploy** لإعادة النشر

---

### خطأ: "Could not find the build directory"

**ما يعنيه:** فشل أمر البناء

**الحل:**
1. راجع **Build Logs** في Render
2. تأكد من أن Build Command هو: `npm install && npm run build`
3. تحقق من عدم وجود أخطاء في الكود
4. تأكد من أن جميع التبعيات في `package.json`

**كيفية الإصلاح:**
1. افتح **Settings** → **Build & Deploy**
2. تحقق من Build Command
3. اضغط **Manual Deploy** → **Clear build cache & deploy**

---

### خطأ: "relation does not exist"

**ما يعنيه:** الجداول غير موجودة في قاعدة البيانات

**الحل:**
1. افتح **Shell** في Render (من قائمة Web Service)
2. شغل:
   ```bash
   npm run db:push
   ```
3. انتظر حتى تكتمل العملية
4. ستظهر رسالة نجاح عند إنشاء جميع الجداول

**بدلاً من ذلك:**
- استخدم Drizzle Studio لإنشاء الجداول يدوياً
- أو شغل migrations من Neon Dashboard

---

### خطأ: "Error: connect ECONNREFUSED" أو "ENOTFOUND"

**ما يعنيه:** لا يمكن الاتصال بقاعدة البيانات

**الحل:**
1. تحقق من أن قاعدة بيانات Neon نشطة
2. راجع DATABASE_URL - يجب أن يكون صحيحاً تماماً
3. تأكد من أن `?sslmode=require` موجود في نهاية الرابط
4. تحقق من أن اسم المستخدم وكلمة المرور صحيحة

**كيفية التحقق:**
1. اذهب إلى Neon Dashboard
2. افتح مشروعك
3. انسخ Connection String مرة أخرى
4. استبدل DATABASE_URL القديم بالجديد في Render

---

### خطأ: "502 Bad Gateway" أو "503 Service Unavailable"

**ما يعنيه:** الخادم لا يستجيب أو متوقف

**الحل:**
1. راجع **Logs** في Render للبحث عن الخطأ
2. تحقق من أن Start Command هو: `npm start`
3. تأكد من أن NODE_ENV=production
4. جرب إعادة النشر يدوياً

**كيفية اختبار Health Check:**
```bash
curl https://your-app-name.onrender.com/api/health
```
إذا أعاد `{"status":"ok",...}` فالخادم يعمل والمشكلة في قاعدة البيانات.

---

### خطأ: "500 Internal Server Error"

**ما يعنيه:** خطأ في الخادم

**الحل:**
1. افتح **Logs** في Render
2. ابحث عن رسالة الخطأ الكاملة
3. أكثر الأسباب شيوعاً:
   - DATABASE_URL غير صحيح
   - الجداول غير موجودة (شغل `npm run db:push`)
   - SESSION_SECRET مفقود

---

### التطبيق بطيء جداً أو يتوقف

**ما يعنيه:** خطة Free في Render تتوقف بعد 15 دقيقة من عدم النشاط

**الحل:**
1. **طبيعي**: هذا سلوك متوقع في الخطة المجانية
2. عند أول زيارة بعد التوقف، قد يستغرق 30-60 ثانية للبدء
3. **للحل الدائم**: استخدم خطة مدفوعة ($7/شهر)
4. **بديل مجاني**: استخدم UptimeRobot لفحص التطبيق كل 5 دقائق

---

## 🔍 كيفية قراءة السجلات (Logs)

### في Render:
1. افتح Web Service الخاص بك
2. انقر على **Logs**
3. ابحث عن:
   - ❌ `Error:` - أخطاء
   - ⚠️ `Warning:` - تحذيرات
   - ✅ `serving on port` - التطبيق يعمل

### فهم الأخطاء:
```
❌ DATABASE_URL must be set
   → أضف DATABASE_URL في Environment

❌ Could not find the build directory
   → شغل Build مرة أخرى

❌ relation "users" does not exist
   → شغل npm run db:push

✅ serving on port 3000
   → التطبيق يعمل بنجاح!
```

---

## 🧪 اختبار النشر

### 1. Health Check
```bash
curl https://your-app.onrender.com/api/health
```
**يجب أن يعيد:**
```json
{"status":"ok","timestamp":"2024-12-07T10:00:00.000Z","version":"1.0.0"}
```

### 2. اختبار الصفحة الرئيسية
1. افتح التطبيق في المتصفح
2. يجب أن ترى الصفحة الرئيسية العربية
3. إذا ظهرت صفحة بيضاء، افتح Developer Tools (F12) وراجع Console

### 3. اختبار تسجيل الدخول
1. اضغط "تسجيل الدخول"
2. أدخل اسمك كمعلم
3. يجب أن تُنقل إلى لوحة التحكم

---

## 📞 الحصول على المساعدة

إذا جربت جميع الحلول أعلاه ولم تنجح:

1. **راجع السجلات بعناية** - 99% من المشاكل موضحة هناك
2. **تأكد من DATABASE_URL** - هذا أكثر سبب شائع
3. **جرب إعادة النشر** من الصفر
4. **تحقق من .env.example** - للتأكد من جميع المتغيرات المطلوبة

### معلومات مفيدة عند طلب المساعدة:
- لقطة شاشة من Build Logs
- لقطة شاشة من Runtime Logs
- لقطة شاشة من Environment Variables (بدون القيم الحساسة!)
- رسالة الخطأ الكاملة

---

## 📚 موارد إضافية

- [DEPLOYMENT.md](./DEPLOYMENT.md) - دليل النشر الكامل
- [README.md](./README.md) - معلومات عامة عن المشروع
- [.env.example](./.env.example) - متغيرات البيئة المطلوبة
- [Render Documentation](https://render.com/docs) - وثائق Render
- [Neon Documentation](https://neon.tech/docs) - وثائق Neon

---

**آخر تحديث:** ديسمبر 2024  
**المطور:** عبدالعزيز الخلفان

# قائمة التحقق من النشر السريعة
# Quick Deployment Checklist

## ✅ قبل النشر (Pre-Deployment)

### 1. إعداد قاعدة البيانات
- [ ] حساب Neon تم إنشاؤه على https://neon.tech
- [ ] مشروع قاعدة بيانات تم إنشاؤه
- [ ] تم نسخ Connection String
- [ ] Connection String يحتوي على `?sslmode=require`

### 2. إعداد متغيرات البيئة
- [ ] `DATABASE_URL` تم تعيينه
- [ ] `SESSION_SECRET` تم إنشاؤه (32+ حرف عشوائي)
- [ ] `NODE_ENV=production` تم تعيينه
- [ ] `PORT` تم تعيينه (3000 أو 5000)

### 3. الاختبار المحلي
- [ ] `npm install` تم تشغيله بنجاح
- [ ] `npm run build` تم تشغيله بنجاح
- [ ] لا توجد أخطاء في البناء

---

## 🚀 أثناء النشر (During Deployment)

### اختر منصة الاستضافة:

#### خيار A: Replit
- [ ] رفع الكود إلى GitHub
- [ ] فتح المشروع في Replit
- [ ] إضافة Secrets (متغيرات البيئة)
- [ ] النقر على زر Publish
- [ ] اختيار Autoscale Deployment

#### خيار B: Render.com
- [ ] رفع الكود إلى GitHub
- [ ] إنشاء Web Service جديد على Render
- [ ] ربط مستودع GitHub
- [ ] إضافة Environment Variables
- [ ] Build Command: `npm install && npm run build`
- [ ] Start Command: `npm start`

---

## 🔧 بعد النشر (Post-Deployment)

### 1. إنشاء جداول قاعدة البيانات
```bash
npm run db:push
```
- [ ] تم تشغيل الأمر بنجاح
- [ ] الجداول تم إنشاؤها في Neon

### 2. التحقق من الصحة
- [ ] رابط التطبيق يعمل
- [ ] الصفحة الرئيسية تظهر بشكل صحيح
- [ ] `/api/health` يعيد `{"status": "ok"}`

### 3. اختبار الوظائف الأساسية
- [ ] تسجيل الدخول يعمل
- [ ] إنشاء حساب مستخدم جديد يعمل
- [ ] إنشاء مؤشر جديد يعمل
- [ ] إضافة معيار يعمل
- [ ] إضافة شاهد يعمل

---

## 👥 إعداد المستخدمين

### 1. إنشاء حساب المنشئ (Creator)
- [ ] تسجيل الدخول لأول مرة
- [ ] تعيين دور "creator" لحسابك في قاعدة البيانات

### 2. إنشاء حساب المدير (Principal)
- [ ] إنشاء حساب للمدير
- [ ] تعيين دور "admin" للمدير

### 3. دعوة المعلمين
- [ ] مشاركة رابط التطبيق
- [ ] توضيح طريقة التسجيل

---

## 🔐 الأمان

### التحقق من الإعدادات الأمنية
- [ ] SESSION_SECRET فريد وآمن
- [ ] DATABASE_URL لا يظهر في السجلات (Logs)
- [ ] ملف .env غير مرفوع إلى GitHub
- [ ] SSL/TLS مفعل (HTTPS)

---

## 📊 المراقبة

### إعداد المراقبة
- [ ] تفعيل السجلات (Logs) في المنصة
- [ ] تفعيل التنبيهات للأخطاء
- [ ] مراقبة استهلاك قاعدة البيانات في Neon
- [ ] مراقبة استهلاك الذاكرة في المنصة

---

## 📝 التوثيق

### مشاركة المعلومات
- [ ] توثيق رابط التطبيق
- [ ] توثيق بيانات الدخول للمدير
- [ ] توثيق طريقة الوصول لقاعدة البيانات
- [ ] مشاركة دليل الاستخدام مع المستخدمين

---

## 🎉 النجاح!

إذا تم تحديد جميع النقاط أعلاه، فإن تطبيقك:
- ✅ منشور ومتاح على الإنترنت
- ✅ آمن ومحمي
- ✅ جاهز للاستخدام
- ✅ مراقب ومتابع

**رابط التطبيق**: __________________

**تاريخ النشر**: __________________

**المسؤول**: عبدالعزيز الخلفان

---

## 🆘 في حالة المشاكل

راجع ملف `DEPLOYMENT.md` لتفاصيل أكثر وحلول المشاكل الشائعة.

### مشاكل شائعة:

1. **التطبيق لا يبدأ**
   → تحقق من `npm run build` محلياً

2. **خطأ في قاعدة البيانات**
   → تحقق من `DATABASE_URL` و `?sslmode=require`

3. **خطأ في الجلسات**
   → تحقق من `SESSION_SECRET`

4. **الجداول غير موجودة**
   → شغّل `npm run db:push`

---

**للدعم الكامل**: راجع `DEPLOYMENT.md`

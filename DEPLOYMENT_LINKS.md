# روابط التشغيل والنشر
## نظام توثيق شواهد الأداء الوظيفي

**آخر تحديث:** 7 ديسمبر 2025

---

## 🌐 خيارات الوصول للتطبيق

يمكن الوصول للتطبيق بثلاث طرق:

### 1️⃣ التشغيل المحلي (Local Development)

**للمطورين والاختبار المحلي:**

```bash
# 1. استنساخ المشروع
git clone https://github.com/alzaem2002-ctrl/zaydbinthabit.git
cd zaydbinthabit

# 2. تثبيت الحزم
npm install

# 3. إعداد قاعدة البيانات (Neon PostgreSQL)
# قم بإنشاء قاعدة بيانات على https://neon.tech
# ثم أنشئ ملف .env وأضف:
# DATABASE_URL=postgresql://...?sslmode=require
# SESSION_SECRET=your-secret-key-here
# NODE_ENV=development
# PORT=5000

# 4. تطبيق schema على قاعدة البيانات
npm run db:push

# 5. تشغيل التطبيق
npm run dev
```

**الرابط المحلي:**
```
http://localhost:5000
```

---

### 2️⃣ النشر على Replit (الأسرع والأسهل) ⭐

**خطوات النشر:**

1. **فتح المشروع على Replit:**
   - قم بزيارة: [https://replit.com](https://replit.com)
   - انقر على "Create Repl"
   - اختر "Import from GitHub"
   - أدخل رابط المشروع: `https://github.com/alzaem2002-ctrl/zaydbinthabit`

2. **إعداد قاعدة البيانات:**
   - قم بإنشاء قاعدة بيانات مجانية على [Neon.tech](https://neon.tech)
   - انسخ رابط الاتصال (Connection String)

3. **إضافة متغيرات البيئة في Replit:**
   - افتح تبويب "Secrets" (القفل 🔒 في الشريط الجانبي)
   - أضف المتغيرات التالية:
     ```
     DATABASE_URL = postgresql://...?sslmode=require
     SESSION_SECRET = any-long-random-string
     NODE_ENV = production
     PORT = 5000
     ```

4. **تطبيق Schema:**
   - في Shell، نفذ: `npm run db:push`

5. **تشغيل التطبيق:**
   - انقر على زر "Run" في الأعلى
   - انتظر حتى يكتمل التشغيل

**الرابط على Replit:**
```
https://[repl-name].[username].repl.co
```

**مثال:**
```
https://zaydbinthabit.alzaem2002.repl.co
```

**مميزات Replit:**
- ✅ نشر فوري في دقائق
- ✅ SSL/TLS تلقائي (HTTPS)
- ✅ لا حاجة لإعدادات معقدة
- ✅ إعادة تشغيل تلقائية
- ✅ محرر كود مدمج

---

### 3️⃣ النشر على Render.com (للإنتاج) 🚀

**خطوات النشر:**

1. **إنشاء حساب:**
   - قم بزيارة [Render.com](https://render.com)
   - سجل دخول بحساب GitHub

2. **إنشاء Web Service جديد:**
   - انقر على "New +" ثم "Web Service"
   - اختر المستودع: `alzaem2002-ctrl/zaydbinthabit`
   - اختر الفرع: `main` أو `copilot/update-project-overview`

3. **إعدادات الخدمة:**
   ```
   Name: zaydbinthabit
   Environment: Node
   Build Command: npm install && npm run build
   Start Command: npm start
   Plan: Free
   ```

4. **إضافة متغيرات البيئة:**
   - في قسم "Environment"، أضف:
     ```
     DATABASE_URL = postgresql://...?sslmode=require
     SESSION_SECRET = [auto-generate]
     NODE_ENV = production
     PORT = 3000
     ```

5. **النشر:**
   - انقر على "Create Web Service"
   - انتظر اكتمال البناء (2-5 دقائق)

**الرابط على Render.com:**
```
https://zaydbinthabit.onrender.com
```

أو حسب الاسم المختار:
```
https://[service-name].onrender.com
```

**مميزات Render.com:**
- ✅ أداء ممتاز للإنتاج
- ✅ SSL/TLS تلقائي
- ✅ نشر تلقائي عند push
- ✅ سجلات (Logs) مفصلة
- ✅ دعم نطاقات مخصصة
- ✅ مجاني حتى 750 ساعة/شهر

---

## 📝 ملاحظات مهمة

### قاعدة البيانات - Neon PostgreSQL (موصى به)

**لماذا Neon؟**
- ✅ PostgreSQL Serverless
- ✅ 3 GB مجاني
- ✅ اتصال سريع وآمن
- ✅ نسخ احتياطية تلقائية

**خطوات إنشاء قاعدة بيانات:**

1. زيارة [https://neon.tech](https://neon.tech)
2. إنشاء حساب مجاني
3. إنشاء مشروع جديد
4. اختيار المنطقة الأقرب (Europe أو US)
5. نسخ "Connection String"
6. **مهم:** تأكد من إضافة `?sslmode=require` في نهاية الرابط

**مثال على Connection String:**
```
postgresql://username:password@ep-xxx.region.neon.tech/neondb?sslmode=require
```

---

## 🔐 الأمان

**متغيرات البيئة المطلوبة:**

1. **DATABASE_URL** (إلزامي)
   - رابط الاتصال بقاعدة البيانات
   - يجب أن ينتهي بـ `?sslmode=require`

2. **SESSION_SECRET** (إلزامي)
   - مفتاح سري لتشفير الجلسات
   - يجب أن يكون عشوائي وطويل (32+ حرف)
   - لتوليد مفتاح: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`

3. **NODE_ENV** (إلزامي)
   - `development` للتطوير
   - `production` للإنتاج

4. **PORT** (اختياري)
   - المنفذ الذي سيعمل عليه التطبيق
   - الافتراضي: 5000 (Replit) أو 3000 (Render)

---

## 🎓 تسجيل الدخول الأولي

بعد نشر التطبيق، يمكنك الدخول بالطرق التالية:

### للمعلمين:
1. انتقل إلى صفحة تسجيل الدخول
2. اختر "معلم"
3. أدخل اسمك الكامل
4. سيتم إنشاء حساب تلقائياً

### للمديرين:
1. انتقل إلى صفحة تسجيل الدخول
2. اختر "مدير"
3. أدخل كلمة المرور الافتراضية: `admin123`
4. **مهم:** قم بتغيير كلمة المرور فوراً بعد الدخول

### لمنشئ الموقع (Creator):
1. انتقل إلى صفحة تسجيل الدخول
2. اختر "منشئ الموقع"
3. أدخل كلمة المرور الافتراضية: `creator123`
4. **مهم:** قم بتغيير كلمة المرور فوراً بعد الدخول

---

## 🔗 الروابط المفيدة

### الوثائق:
- 📋 [حالة المشروع](PROJECT_STATUS.md)
- 🎯 [محاكاة تجربة المستخدم](UX_SIMULATION.md)
- ✅ [تقرير التحقق من البناء](BUILD_VERIFICATION.md)
- 📖 [دليل الاستخدام](README.md)

### المنصات:
- 🔗 [Replit](https://replit.com)
- 🔗 [Render.com](https://render.com)
- 🔗 [Neon Database](https://neon.tech)
- 🔗 [GitHub Repository](https://github.com/alzaem2002-ctrl/zaydbinthabit)

---

## ❓ استكشاف الأخطاء

### المشكلة: لا يمكن الاتصال بقاعدة البيانات

**الحل:**
1. تأكد من صحة `DATABASE_URL`
2. تأكد من وجود `?sslmode=require` في نهاية الرابط
3. تأكد من أن قاعدة البيانات نشطة على Neon
4. نفذ `npm run db:push` لإنشاء الجداول

### المشكلة: خطأ في SESSION_SECRET

**الحل:**
1. تأكد من وجود `SESSION_SECRET` في متغيرات البيئة
2. تأكد من أن المفتاح طويل بما يكفي (32+ حرف)

### المشكلة: صفحة فارغة أو خطأ 404

**الحل:**
1. تأكد من اكتمال البناء بنجاح
2. تحقق من سجلات الخادم (Logs)
3. تأكد من أن جميع الحزم مثبتة
4. جرب `npm run build` ثم `npm start`

---

## 📞 الدعم

للمساعدة الفنية:
1. راجع [BUILD_VERIFICATION.md](BUILD_VERIFICATION.md)
2. راجع [README.md](README.md) - قسم المساعدة
3. افتح Issue في GitHub
4. تواصل مع المطور

---

**الحالة:** ✅ التطبيق جاهز للنشر والتشغيل  
**آخر تحديث:** 7 ديسمبر 2025  
**الإصدار:** 1.0.0

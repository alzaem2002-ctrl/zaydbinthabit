# Database Configuration
# Get a free PostgreSQL database from: https://neon.tech
DATABASE_URL=postgresql://username:password@host.neon.tech/dbname?sslmode=require

# Session Secret
# Generate a random string for production: openssl rand -base64 32
SESSION_SECRET=your-random-secret-key-here-change-this-in-production

# Application Configuration
NODE_ENV=production
PORT=3000

# OpenID Connect Authentication (Optional - for Replit OAuth)
# Only needed if using Replit OAuth authentication
OPENID_CLIENT_ID=your-replit-client-id
OPENID_CLIENT_SECRET=your-replit-client-secret
OPENID_ISSUER=https://replit.com

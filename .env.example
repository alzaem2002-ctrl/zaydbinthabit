# نموذج متغيرات البيئة
# Environment Variables Template
# انسخ هذا الملف إلى .env وعدّل القيم

# قاعدة البيانات (إجباري)
# Database URL - Required
# احصل عليه من https://neon.tech
DATABASE_URL=postgresql://username:password@ep-xxx.neon.tech/neondb?sslmode=require

# مفتاح الجلسات (إجباري)
# Session Secret - Required
# أنشئ مفتاح عشوائي آمن (32 حرف أو أكثر)
# استخدم: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-here-32-chars-minimum

# بيئة التشغيل (إجباري)
# Node Environment - Required
NODE_ENV=production

# المنفذ (إجباري)
# Port - Required
PORT=5000

# ======================================
# متغيرات اختيارية للمصادقة عبر OAuth
# Optional OAuth Variables
# ======================================

# معرّف تطبيق OAuth (اختياري)
# OAuth Client ID - Optional
OPENID_CLIENT_ID=

# مفتاح تطبيق OAuth السري (اختياري)
# OAuth Client Secret - Optional
OPENID_CLIENT_SECRET=

# رابط مزود OAuth (اختياري)
# OAuth Issuer URL - Optional
OPENID_ISSUER=https://replit.com

# ======================================
# ملاحظات مهمة / Important Notes
# ======================================

# 1. لا تشارك ملف .env أبداً في GitHub
#    Never commit .env file to GitHub
#
# 2. استخدم قيم مختلفة للتطوير والإنتاج
#    Use different values for development and production
#
# 3. DATABASE_URL يجب أن ينتهي بـ ?sslmode=require
#    DATABASE_URL must end with ?sslmode=require
#
# 4. SESSION_SECRET يجب أن يكون عشوائي وآمن
#    SESSION_SECRET must be random and secure

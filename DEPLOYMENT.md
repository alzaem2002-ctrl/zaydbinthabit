# دليل النشر - نظام توثيق شواهد الأداء الوظيفي

## 📋 قائمة التحقق من النشر

قبل نشر التطبيق، تأكد من إكمال جميع الخطوات التالية:

- [ ] إنشاء قاعدة بيانات على Neon.tech
- [ ] إضافة DATABASE_URL في متغيرات البيئة
- [ ] إضافة SESSION_SECRET في متغيرات البيئة
- [ ] تفعيل Build Command و Start Command بشكل صحيح
- [ ] اختبار الاتصال بقاعدة البيانات
- [ ] تشغيل db:push لإنشاء الجداول

---

## ⚠️ الأخطاء الشائعة في النشر

### خطأ: "DATABASE_URL must be set"

**السبب:** لم يتم تعيين متغير البيئة DATABASE_URL

**الحل:**
1. انتقل إلى لوحة تحكم منصة النشر (Render.com)
2. افتح إعدادات Web Service الخاص بك
3. اذهب إلى قسم "Environment"
4. أضف متغير جديد:
   - **Key**: `DATABASE_URL`
   - **Value**: رابط الاتصال من Neon.tech

### خطأ: "Could not find the build directory"

**السبب:** فشل أمر البناء (build command)

**الحل:**
1. تأكد من أن Build Command في Render هو: `npm install && npm run build`
2. تحقق من أن جميع التبعيات (dependencies) مثبتة بشكل صحيح
3. راجع سجلات البناء (build logs) لمعرفة الخطأ المحدد

### خطأ: "Port already in use" أو تعذر الاتصال

**السبب:** المنفذ (PORT) غير صحيح

**الحل:**
1. تأكد من أن متغير PORT يساوي 3000 في Render
2. التطبيق يستخدم تلقائياً `process.env.PORT` أو يعود إلى 5000

---

## 🚀 النشر على Render.com (موصى به)

### الخطوة 1: إنشاء قاعدة بيانات على Neon.tech

1. قم بزيارة [https://neon.tech](https://neon.tech)
2. سجل دخول أو أنشئ حساب مجاني
3. انقر على **"New Project"**
4. أدخل اسم المشروع (مثل: `school-performance-db`)
5. اختر المنطقة الأقرب لك (يفضل `US East` للسرعة)
6. انقر على **"Create Project"**
7. **انسخ Connection String** من لوحة التحكم:
   ```
   postgresql://username:password@ep-xxxx.neon.tech/neondb?sslmode=require
   ```
8. احتفظ بهذا الرابط - ستحتاجه في الخطوة التالية

### الخطوة 2: إنشاء Web Service على Render.com

1. قم بزيارة [https://render.com](https://render.com)
2. سجل دخول أو أنشئ حساب مجاني
3. انقر على **"New +"** ثم اختر **"Web Service"**
4. اربط حسابك مع GitHub إذا لم يكن مربوطاً
5. ابحث عن المستودع `zaydbinthabit` واختره
6. انقر على **"Connect"**

### الخطوة 3: إعداد Web Service

أدخل الإعدادات التالية:

**Basic Settings:**
- **Name**: `school-performance-system` (أو أي اسم تريده)
- **Region**: اختر المنطقة الأقرب لك
- **Branch**: `main` (أو الفرع الرئيسي)
- **Root Directory**: اتركه فارغاً
- **Environment**: `Node`
- **Build Command**: 
  ```bash
  npm install && npm run build
  ```
- **Start Command**:
  ```bash
  npm start
  ```
- **Plan**: اختر **Free** للبداية

### الخطوة 4: إضافة متغيرات البيئة (Environment Variables)

في قسم "Environment Variables"، أضف المتغيرات التالية:

1. **DATABASE_URL** (مطلوب):
   - انقر على **"Add Environment Variable"**
   - **Key**: `DATABASE_URL`
   - **Value**: الصق رابط Connection String من Neon.tech
   - يجب أن ينتهي بـ `?sslmode=require`

2. **SESSION_SECRET** (مطلوب):
   - **Key**: `SESSION_SECRET`
   - **Value**: نص عشوائي طويل (يمكن توليده بأمر: `openssl rand -base64 32`)
   - مثال: `xK9mP2vN8qR4tY6wE3sA7dF5gH1jL0kI9uY8tR6eW4qP2oL5mK3nB7vC1xZ4aS6`

3. **NODE_ENV** (مطلوب):
   - **Key**: `NODE_ENV`
   - **Value**: `production`

4. **PORT** (اختياري - Render يضبطه تلقائياً):
   - **Key**: `PORT`
   - **Value**: `3000`

5. **OPENID_CLIENT_ID** (اختياري - فقط إذا كنت تستخدم Replit OAuth):
   - **Key**: `OPENID_CLIENT_ID`
   - **Value**: معرف العميل من Replit

6. **OPENID_CLIENT_SECRET** (اختياري):
   - **Key**: `OPENID_CLIENT_SECRET`
   - **Value**: سر العميل من Replit

7. **OPENID_ISSUER** (اختياري):
   - **Key**: `OPENID_ISSUER`
   - **Value**: `https://replit.com`

### الخطوة 5: إنشاء الجداول في قاعدة البيانات

بعد نشر التطبيق بنجاح:

1. افتح Shell في Render (من قائمة Web Service)
2. شغل الأمر التالي لإنشاء الجداول:
   ```bash
   npm run db:push
   ```
3. انتظر حتى تكتمل العملية (سيتم إنشاء 10 جداول)

### الخطوة 6: إنشاء الخدمة ونشر التطبيق

1. راجع جميع الإعدادات للتأكد من صحتها
2. انقر على **"Create Web Service"**
3. انتظر حتى يكتمل البناء والنشر (قد يستغرق 3-5 دقائق)
4. عند اكتمال النشر، ستحصل على رابط التطبيق:
   ```
   https://school-performance-system.onrender.com
   ```

### الخطوة 7: الاختبار

1. **اختبار Health Check** (للتحقق من أن الخادم يعمل):
   ```bash
   curl https://your-app-name.onrender.com/api/health
   ```
   يجب أن ترى: `{"status":"ok","timestamp":"...","version":"1.0.0",...}`

2. افتح رابط التطبيق في المتصفح
3. تحقق من أن الصفحة الرئيسية تظهر بشكل صحيح
4. جرب تسجيل الدخول كمعلم
5. تحقق من إنشاء مؤشر جديد

**ملاحظة**: إذا لم تعمل الصفحة الرئيسية لكن `/api/health` يعمل، فالمشكلة غالباً في قاعدة البيانات. راجع DATABASE_URL.

---

## 🔧 استكشاف الأخطاء

### كيفية عرض السجلات (Logs)

1. في لوحة تحكم Render، افتح Web Service الخاص بك
2. انقر على تبويب **"Logs"**
3. راجع السجلات للبحث عن الأخطاء

### الأخطاء الشائعة والحلول

#### 1. "Error: connect ECONNREFUSED"
**السبب:** لا يمكن الاتصال بقاعدة البيانات

**الحل:**
- تحقق من أن DATABASE_URL صحيح
- تأكد من أن رابط Neon يحتوي على `?sslmode=require`
- تحقق من أن قاعدة البيانات على Neon نشطة

#### 2. "relation does not exist"
**السبب:** الجداول غير موجودة في قاعدة البيانات

**الحل:**
- شغل `npm run db:push` من Shell في Render
- أو استخدم Drizzle Studio لإنشاء الجداول

#### 3. "500 Internal Server Error"
**السبب:** خطأ في الخادم

**الحل:**
- راجع السجلات (Logs) في Render
- تحقق من جميع متغيرات البيئة
- تأكد من أن Build نجح بدون أخطاء

#### 4. التطبيق بطيء أو يتوقف
**السبب:** خطة Free في Render تتوقف بعد 15 دقيقة من عدم النشاط

**الحل:**
- استخدم خطة مدفوعة للحصول على خدمة دائمة
- أو استخدم خدمة Uptime Monitor مثل UptimeRobot

---

## 🔄 تحديث التطبيق بعد النشر

عند إجراء تغييرات على الكود:

1. ادفع التغييرات إلى GitHub:
   ```bash
   git add .
   git commit -m "وصف التغيير"
   git push origin main
   ```

2. Render سيكتشف التغييرات تلقائياً ويبدأ Build جديد

3. انتظر حتى يكتمل النشر (سترى "Build successful" في Logs)

---

## 🌐 النشر على منصات أخرى

### Vercel

Vercel يدعم Next.js بشكل رئيسي، لكن يمكن استخدامه مع Express:

1. قم بتثبيت Vercel CLI:
   ```bash
   npm install -g vercel
   ```

2. شغل:
   ```bash
   vercel
   ```

3. اتبع التعليمات لربط المشروع

**ملاحظة:** قد تحتاج لتعديل الإعدادات لدعم Express بشكل كامل.

### Railway

Railway مشابه لـ Render:

1. قم بزيارة [https://railway.app](https://railway.app)
2. اربط GitHub repository
3. أضف متغيرات البيئة
4. انشر التطبيق

### Heroku

Heroku يتطلب خطة مدفوعة الآن:

1. قم بإنشاء `Procfile`:
   ```
   web: npm start
   ```

2. استخدم Heroku Postgres أو Neon
3. اتبع دليل Heroku لنشر Node.js

---

## 📊 المراقبة والصيانة

### مراقبة الأداء

- استخدم لوحة التحكم في Render لمراقبة الأداء
- راقب استخدام الذاكرة (Memory Usage)
- راقب وقت الاستجابة (Response Time)

### النسخ الاحتياطية

في Neon.tech:
1. اذهب إلى Project Settings
2. فعّل Automatic Backups
3. يمكنك أيضاً إنشاء نسخة احتياطية يدوية

### التحديثات الأمنية

- راجع `npm audit` بانتظام:
  ```bash
  npm audit
  npm audit fix
  ```

- حدّث التبعيات:
  ```bash
  npm update
  ```

---

## 💡 نصائح للأداء الأفضل

1. **استخدم CDN**: لتسريع تحميل الأصول الثابتة
2. **فعّل Caching**: لتقليل الحمل على الخادم
3. **راقب السجلات**: لاكتشاف المشاكل مبكراً
4. **استخدم خطة مدفوعة**: للحصول على أداء أفضل واستقرار أكبر

---

## 📞 الدعم

إذا واجهت مشاكل:

1. راجع قسم "استكشاف الأخطاء" أعلاه
2. تحقق من السجلات (Logs) في Render
3. راجع README.md للمزيد من التفاصيل
4. تأكد من جميع متغيرات البيئة المطلوبة

---

**آخر تحديث:** ديسمبر 2024  
**المطور:** عبدالعزيز الخلفان
